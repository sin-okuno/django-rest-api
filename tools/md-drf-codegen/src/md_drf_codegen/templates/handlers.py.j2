# AUTO-GENERATED FILE.
# Generated by md-drf-codegen from YAML API specification.
# Do not edit this file directly; edit the source Markdown / YAML instead.
# title: {{ title }}
# source: {{ source }}
#
# View は Handler クラスを直接インスタンス化して handle() を呼ぶ。
# GET 系はデモデータを返す。それ以外は NotImplementedError（業務実装待ち）。

from __future__ import annotations

from typing import Any

{% for handler in handlers %}

class {{ handler.class_name }}:
    """{{ handler.description }} (api_id={{ handler.api_id }})"""

    api_id = {{ handler.api_id | tojson }}

    def handle(
        self,
        *,
        request: Any,
{% if handler.has_validated_data %}
        validated_data: dict[str, Any],
{% endif %}
{% for param in handler.path_params %}
        {{ param }}: str,
{% endfor %}
    ) -> Any:
{% if handler.is_get %}
        """Return demo data for api_id={{ handler.api_id }} (temporary)."""
        return {{ handler.demo_payload | topython }}
{% else %}
        """Implement business logic for api_id={{ handler.api_id }} here."""
        raise NotImplementedError(
            f'Handler for API ID "{self.api_id}" is not implemented yet.'
        )
{% endif %}

{% endfor %}
